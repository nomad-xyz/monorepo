{
  "name": "@nomad-xyz/monorepo",
  "private": true,
  "workspaces": [
    "packages/contracts-bridge",
    "packages/contracts-core",
    "packages/contracts-router",
    "packages/contracts-fork-tests",
    "packages/deploy",
    "packages/multi-provider",
    "packages/local-environment",
    "packages/sdk",
    "packages/sdk-bridge",
    "packages/sdk-govern",
    "packages/indexer",
    "packages/keymaster"
  ],
  "scripts": {
    "compile": "yarn workspaces foreach -Apv --exclude @nomad-xyz/deploy --exclude @nomad-xyz/local-environment run compile",
    "bootstrap": "yarn build:accumulator-cli && yarn workspaces foreach -Apv --exclude @nomad-xyz/deploy --exclude @nomad-xyz/local-environment run bootstrap",
    "build": "yarn build:accumulator-cli && yarn workspaces foreach -Apv --exclude @nomad-xyz/deploy --exclude @nomad-xyz/local-environment run build",
    "build:accumulator-cli": "cd scripts && ./build-acli.sh",
    "contracts-bridge": "yarn workspace @nomad-xyz/contracts-bridge",
    "contracts-core": "yarn workspace @nomad-xyz/contracts-core",
    "contracts-router": "yarn workspace @nomad-xyz/contracts-router",
    "deploy": "yarn workspace @nomad-xyz/deploy",
    "indexer": "yarn workspace @nomad-xyz/indexer",
    "lint": "yarn workspaces foreach -Apv --exclude @nomad-xyz/indexer --exclude @nomad-xyz/local-environment run lint",
    "local-environment": "yarn workspace @nomad-xyz/local-environment",
    "multi-provider": "yarn workspace @nomad-xyz/multi-provider",
    "prettier": "yarn workspaces foreach -Apv --exclude @nomad-xyz/local-environment run prettier",
    "sdk": "yarn workspace @nomad-xyz/sdk",
    "sdk-bridge": "yarn workspace @nomad-xyz/sdk-bridge",
    "sdk-govern": "yarn workspace @nomad-xyz/sdk-govern",
    "test": "yarn workspaces foreach -Apv --exclude @nomad-xyz/deploy  --exclude @nomad-xyz/local-environment run test",
    "gen-proof": "scripts/accumulator-cli",
    "snapshot": "yarn workspaces foreach -Apv --include @nomad-xyz/contracts-core --include @nomad-xyz/contracts-bridge run snapshot",
    "snapshot:check": "yarn workspaces foreach -Apv --include @nomad-xyz/contracts-core --include @nomad-xyz/contracts-bridge run snapshot:check",
    "storage-inspect:generate": "yarn workspaces foreach -Apv --include @nomad-xyz/contracts-core --include @nomad-xyz/contracts-bridge run storage-inspect:generate",
    "storage-inspect:check": "yarn workspaces foreach -Apv --include @nomad-xyz/contracts-core --include @nomad-xyz/contracts-bridge run storage-inspect:check"
  },
  "packageManager": "yarn@3.2.0"
}
