{
  "private": true,
  "name": "@nomad-xyz/contracts",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bridge": "yarn workspace @nomad-xyz/bridge-contracts",
    "core": "yarn workspace @nomad-xyz/core-contracts",
    "deploy": "yarn workspace @nomad-xyz/deploy",
    "example-xapps": "yarn workspace @nomad-xyz/example-xapp-contracts",
    "router": "yarn workspace @nomad-xyz/router-contracts",
    "prettier": "yarn deploy prettier && yarn core prettier && yarn router prettier && yarn bridge prettier && yarn example-xapps prettier",
    "lint": "yarn deploy lint && yarn core lint && yarn router lint && yarn bridge lint && yarn example-xapps lint",
    "compile": "yarn core compile && yarn router compile && yarn bridge compile",
    "build": "yarn lint && yarn prettier && yarn compile"
  }
}
