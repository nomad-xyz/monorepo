import { describe, it } from 'mocha';

import { expect } from 'chai';

describe('sdk-govern', async () => {
  // before(async () => {
  //
  // });

  // beforeEach(async () => {
  //
  // });

  it('', async () => {
    expect(true).to.be.true;
  });
});
