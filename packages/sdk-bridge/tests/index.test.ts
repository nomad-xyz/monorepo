import { describe, it } from 'mocha';

import { expect } from 'chai';

describe('sdk-bridge', async () => {
  // before(async () => {
  //
  // });

  // beforeEach(async () => {
  //
  // });

  it('', async () => {
    expect(true).to.be.true;
  });
});
