import { expect } from 'chai';

describe('multi-provider', async () => {
  // before(async () => {
  //
  // });

  // beforeEach(async () => {
  //
  // });

  it('', async () => {
    expect(true).to.be.true;
  });
});
